# Heavier ML / backtest stack — installed in later phases (3+).
# Phase 1 (the engine) does not need these.
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
optuna>=3.6
shap>=0.45
vectorbt>=0.26
pandas-market-calendars>=4.4
